{
	"name": "scala-webgpu",
	"type": "module",
	"scripts": {
		"build": "scala-cli --power package . --js -o public/out -f",
		"watch": "scala-cli --power package . --js -o public/out -f -w",
		"dev": "bun run watch & bun run serve",
		"serve": "bun --bun serve.ts",
		"test": "scala-cli test ."
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"dependencies": {
		"@webgpu/types": "^0.1.69"
	}
}
